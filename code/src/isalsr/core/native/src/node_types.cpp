/// node_types.cpp — Token grammar implementation unit.
///
/// All functions in node_types.hpp are inline, so this translation unit
/// exists solely to ensure the header compiles as a standalone unit and
/// to anchor the symbol in the shared object for debugger visibility.

#include <isalsr/node_types.hpp>
