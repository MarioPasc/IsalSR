# UDFS debug config: fast smoke test (60s max per problem)
experiment:
  name: debug_udfs_nguyen
  method: udfs
  n_seeds: 1

benchmarks:
  nguyen:
    problems: all
    train_size: 20
    test_size: 100

udfs:
  n_calc_nodes: 3
  max_orders: 1000
  max_time: 60
  processes: 1
  mode: hierarchical
  k: 2
  stop_thresh: 1.0e-10
  use_tan: false
  operator_set: ["+", "*", "-", "/", "sin", "cos", "exp", "log"]

isalsr:
  canonicalization_timeout: 10.0
  use_pruned: true

output:
  base_dir: /tmp/isalsr_debug
