# Pinned Python dependencies for the IsalSR Code Ocean capsule.
#
# Every version is the one the reported results were produced with, recovered
# from `pip freeze` on the development environment (conda env `isalsr`,
# Python 3.11.15, GCC 12.2.0).  Pins are exact: a reproducible run must not
# resolve a different wheel than the one certified here.
#
# Installed by environment/postInstall at IMAGE BUILD time.  A reproducible run
# has no network, so nothing may be resolved later than this file.
#
# Three entries need a non-default source and are therefore NOT listed here;
# postInstall installs them explicitly:
#   torch  ...  CPU wheel, from https://download.pytorch.org/whl/cpu
#   critdd ...  not on PyPI, from git at a pinned commit
#   isalsr ...  the capsule's own code, built from /code at run time

# --- build backend for the C++ extension -----------------------------------
# The extension is compiled from /code during the run, when there is no network,
# so the build backend must already be present and the build must be invoked
# with --no-build-isolation.  scikit-build-core is absent from the development
# `pip freeze` precisely because pip fetched it into a throwaway isolated
# environment there; here it has to be a first-class dependency.
scikit-build-core==0.11.6
nanobind==2.13.0
ninja==1.13.0

# --- core scientific stack --------------------------------------------------
numpy==2.4.3
scipy==1.17.1
sympy==1.14.0
mpmath==1.3.0
networkx==3.6.1
pandas==3.0.1
scikit-learn==1.8.0
h5py==3.16.0
joblib==1.5.3
threadpoolctl==3.6.0

# --- statistics and post-hoc tests -----------------------------------------
statsmodels==0.14.6
scikit-posthocs==0.12.0
patsy==1.0.2

# --- plotting ---------------------------------------------------------------
matplotlib==3.10.8
seaborn==0.13.2
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0
pillow==12.1.1
pyparsing==3.3.2
pygraphviz==1.14

# --- host solvers -----------------------------------------------------------
bingo-nasa==0.5.7
smcpy==0.1.12
dill==0.4.1
mpi4py==4.1.1
pybind11==3.0.2
pybind11-global==3.0.2

# --- experiment support -----------------------------------------------------
stopit==1.1.2
zss==1.2.0
tqdm==4.67.3
PyYAML==6.0.3
python-dateutil==2.9.0.post0
six==1.17.0
sortedcontainers==2.4.0
filelock==3.20.0
fsspec==2025.12.0
Jinja2==3.1.6
MarkupSafe==3.0.2
packaging==25.0
typing_extensions==4.15.0
Pygments==2.19.2

# --- test, lint and type-check ---------------------------------------------
pytest==9.0.2
pytest-cov==7.0.0
coverage==7.13.5
hypothesis==6.151.9
iniconfig==2.3.0
pluggy==1.6.0
ruff==0.15.6
mypy==1.19.1
mypy_extensions==1.1.0
librt==0.8.1
pathspec==1.0.4
setuptools==80.10.2
wheel==0.46.3
